# CyberRiskInsurance

git checkout --theirs .env.template
git checkout --theirs frontend/package-lock.json
git checkout --theirs frontend/src/App.jsx
git checkout --theirs frontend/src/components/AdminLogsPanel.jsx
git add .
git commit -m "Merge Shivam latest changes"
